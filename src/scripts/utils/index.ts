import { EnterTheDuck } from './zoo-header/enter-the-duck';

import { siteInfo } from './site-info/site-info';
import { versionScramble } from './version-scramble/version-scramble';
import { wait } from './wait/wait';

export {
    EnterTheDuck as ZooHeader,
    siteInfo,
    versionScramble,
    wait,
};
